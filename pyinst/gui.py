"""
This is to be the (one) file that has the GUI code? I guess I'm still not sure
When to use files and when to use folders. For a project of this size, I am
going to guess that most python files should be in this folder.
"""

class PyInstPolySynth:
    def __init__(self, env):
        environment = env
    


def make_gui(env):
    return PyInstPolySynth(env)
    

